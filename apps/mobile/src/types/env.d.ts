/** Types for the `@env` module provided by react-native-dotenv (see babel.config.js). */
declare module '@env' {
  export const API_URL: string;
  export const MAP_PROVIDER: string;
  export const GOOGLE_MAPS_API_KEY: string;
}
