# Copy to `.env` and fill in. Loaded by Expo CLI into app.config.ts at build
# time and surfaced to the app via `extra` (read with expo-constants).
API_URL=http://localhost:4000/v1
MAP_PROVIDER=osm
GOOGLE_MAPS_API_KEY=
OLA_MAPS_API_KEY=
