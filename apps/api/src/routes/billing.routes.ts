import { Router } from 'express';

/**
 * Billing routes (store_owner only) — Razorpay subscriptions.
 * Placeholder — endpoints are implemented in a later prompt (Prompt 17).
 */
const router = Router();

export const billingRouter = router;
